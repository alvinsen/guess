machine:

  timezone:
    Asia/Shanghai

  language: python

  python:
    virtualenv: py27


dependencies:
  pre:
    - echo ls -alh /python
    - env
  post:
    - pip install -r requirements.txt
    - python setup.py bdist_wheel

compile:
  override:
    - python ./setup.py build

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123
