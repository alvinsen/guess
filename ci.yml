machine:

  timezone:
    Asia/Shanghai

  language: python

  python:
    virtualenv: py34


dependencies:
  pre:
    - echo -e "\e[31;2m$(date)\e[31;0m"
    - env TZ=Asia/Shanghai echo -e "\e[31;4m$(date)\e[31;0m"
    - ls -alh /etc/profile.d
    - ls -alh /python
    - env
    - echo "*** current python version $(python --version) ***"

compile:
  post:
    - pip install wheel

test:
  override:
    - echo lets fail
    - ls /a/none/exists/file

package:
  override:
    - python ./setup.py bdist_wheel
    - ls -alh dist

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123
