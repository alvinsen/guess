machine:

  timezone:
    Asia/Shanghai

  language: python

  python:
    virtualenv: py34


dependencies:
  pre:
    - ls -alh /etc/profile.d
    - ls -alh /python
    - env
  post:
    - pip install -r requirements.txt
    - python setup.py bdist

compile:
  override:
    - python ./setup.py build

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123
