machine:

  timezone:
    Asia/Shanghai

  language: python

  python:
    virtualenv: py27


dependencies:
  pre:
    - pip install -r requirements.txt
  override:
    - python ./setup.py build
  post:
    - python setup.py bdist_wheel

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123
