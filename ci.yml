machine:

  timezone:
    Asia/Shanghai

  language: python

  python:
    virtualenv: py34


dependencies:
  pre:
    - ls -alh /etc/profile.d
    - ls -alh /python
    - env
    - echo "*** current python version $(python --version) ***"

compile:
  post:
    - python ./setup.py bdist
    - find build -exec ls -alh {} \;

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123
